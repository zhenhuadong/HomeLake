three types design patterns: 

Creational:
目的是在业务逻辑中隐藏具体创建的类本身，只用抽象类来表示
1. AbstractFactory
抽象工厂类  提供一个生产 抽象对象的方法， 具体对象类被隐藏了
AbstractFactory af = new ConcreteFactory()
AbsctractProduct ap = af.createProduct()
ap.doSomething()

AbstractClothesFacotry acf = new OldNavyClothesFaatory
AbstractClothes   ac = acf.createClothes()
ac.getPrice()


2. FatoryMethod


3. Singleton
4. Builder
5. Prototype


structural, 



behavioral

